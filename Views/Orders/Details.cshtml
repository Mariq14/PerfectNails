@using ProjectPrettyNails.Data
@model ProjectPrettyNails.Data.Order

@{
    ViewData["Title"] = "Details";
}

<h1>Детайли</h1>

<div>
    <h4>Поръчка</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @*@Html.DisplayNameFor(model => model.Articuls)*@
            Продукт
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Articuls.Id)
        </dd>
        <dt class="col-sm-2">
            @*@Html.DisplayNameFor(model => model.Quantity)*@
            Количество
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Quantity)
        </dd>
        <dt class="col-sm-2">
            @*@Html.DisplayNameFor(model => model.Users)*@
            Потребител
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(modelItem => modelItem.Users.UserName)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">Промени</a> 
}


    <a asp-action="Index">Назад</a>
</div>
